# Project Rules

## Express Routes

Use chained route definitions with `router.route()`:

```typescript
router.route("/").get(getAll).post(create);
router.route("/:id").get(getOne).put(update).delete(remove);
```
